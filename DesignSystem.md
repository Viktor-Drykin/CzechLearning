# Дизайн-система CzechVocab

Источник — холст макетов iOS MVP (17 экранов). Этот файл — единственный источник правды по
цветам, типографике и метрикам. Значения в коде берутся из `DesignSystem/Colors.swift`,
`DesignSystem/Typography.swift`, `DesignSystem/Metrics.swift`; хардкод hex и «магических» чисел
во вьюхах не допускается.

Направление — нативный iOS: системные цвета и SF Pro, сгруппированные списки с инсетами,
единственный акцент (системный синий). Всё остальное — семантика: зелёный, оранжевый, красный
и синий закреплены за четырьмя оценками SM-2 и не используются декоративно.

---

## 1. Цвета

Каждый токен динамический: значение выбирается по `userInterfaceStyle`, поэтому тёмная тема
не требует дублирования вьюх и отдельных ветвлений.

### 1.1. Фон и поверхности

| Токен | Светлая | Тёмная | Где применяется |
|---|---|---|---|
| `background` | `#F2F2F7` | `#000000` | фон всех экранов |
| `surface` | `#FFFFFF` | `#1C1C1E` | карточки, группы списка, таб-бар в тёмной |
| `surfaceSecondary` | `#F7F7FA` | `#2C2C2E` | блок примера внутри карточки |
| `fill` | `#E4E4E9` | `#2C2C2E` | вторичные кнопки, трек сегментов, поле поиска |
| `fillSecondary` | `#EDEDF2` | `#2C2C2E` | плейсхолдер картинки, фон мелких иконок |
| `tabBar` | `#F9F9F9` | `#1C1C1E` | подложка таб-бара |

### 1.2. Разделители и треки

| Токен | Светлая | Тёмная | Где применяется |
|---|---|---|---|
| `separator` | `#E5E5EA` | `#38383A` | разделители строк списка, инсет 16 pt слева |
| `separatorStrong` | `#D8D8DC` | `#38383A` | верхняя граница таб-бара, низ нав-бара |
| `track` | `#E0E0E5` | `#38383A` | незаполненная часть прогресс-бара сессии |

### 1.3. Текст

| Токен | Светлая | Тёмная | Где применяется |
|---|---|---|---|
| `label` | `#1C1C1E` | `#FFFFFF` | основной текст, чешское слово, перевод |
| `labelSecondary` | `#6E6E73` | `#A1A1A6` | переводы примеров, подписи под числами |
| `labelTertiary` | `#8A8A8E` | `#8E8E93` | заголовки секций, счётчики, служебные пояснения |
| `labelQuaternary` | `#AEAEB2` | `#6E6E73` | глиф и подпись плейсхолдера картинки |
| `chevron` | `#C7C7CC` | `#58585C` | шеврон-дисклоужер в строках списка |
| `onAccent` | `#FFFFFF` | `#FFFFFF` | текст на акцентной заливке |

Контраст: `labelTertiary` на `surface` даёт 4,6:1 — это минимум, ниже опускаться нельзя.
`labelQuaternary` допустим только на плейсхолдере, где не несёт смысла.

### 1.4. Акцент и статусы

| Токен | Светлая | Тёмная | Где применяется |
|---|---|---|---|
| `accent` | `#007AFF` | `#0A84FF` | главные кнопки, активная вкладка, ссылки, число повторений |
| `accentPressed` | `#0062CC` | `#409CFF` | нажатое состояние акцента |
| `accentTint` | `#F1F7FF` | `#0A2647` | фон круглой кнопки озвучки |
| `success` | `#34C759` | `#30D158` | выучено, прогресс ≥ 40%, число новых слов |
| `successTint` | `#E8F8EC` | `#10301A` | фон иконки в пустом состоянии |
| `warning` | `#FF9500` | `#FF9F0A` | в изучении, прогресс 10–40%, рамка поля с опечаткой |
| `warningTint` | `#FFF3E3` | `#3A2A0C` | бейдж «2 промаха» |
| `danger` | `#FF3B30` | `#FF453A` | трудные слова, сброс прогресса, неверный ответ |
| `dangerTint` | `#FFF0EE` | `#3A1714` | фон иконки «Трудные слова» |
| `indigo` | `#5856D6` | `#5E5CE6` | раздел «Фразы» |
| `indigoTint` | `#EFF1FF` | `#1E1E42` | фон иконки раздела «Фразы» |
| `purple` | `#AF52DE` | `#BF5AF2` | режим «Пары» |

### 1.5. Callout примечания (ложные друзья)

38 записей с `note` показываются одинаково во всех режимах — янтарным блоком с рамкой.

| Токен | Светлая | Тёмная |
|---|---|---|
| `noteBackground` | `#FFF6E5` | `#3A2A0C` |
| `noteBorder` | `#F4DFB4` | `#5C4416` |
| `noteLabel` | `#7A4E00` | `#F0C46A` |

### 1.6. Цвета оценок SM-2

Ключевой набор: повторяется на обороте карточки, в cloze-формате и в подсказках после
автоматических режимов. Порядок слева направо всегда `again → hard → good → easy`.

| Оценка | Фон (св.) | Текст (св.) | Интервал (св.) | Фон (тём.) | Текст (тём.) |
|---|---|---|---|---|---|
| `again` | `#FFECEB` | `#D8372B` | `#C7362C` | `#3A1714` | `#FF6961` |
| `hard` | `#FFF3E3` | `#A66A00` | `#9A6100` | `#3A2A0C` | `#FFB340` |
| `good` | `#E6F6EA` | `#1D8139` | `#1C7A36` | `#12341C` | `#4CD964` |
| `easy` | `#E8F0FE` | `#0062CC` | `#0057B8` | `#12294A` | `#409CFF` |

### 1.7. График активности

Столбец окрашивается по доле от максимума за 30 дней: < 55% — `barLow`, 55–80% — `barMid`,
> 80% — `barHigh`. День без ответов — полоска 2 pt цветом `barEmpty`.

| Токен | Светлая | Тёмная |
|---|---|---|
| `barLow` | `#B8D8FF` | `#1D4A82` |
| `barMid` | `#8FC1FF` | `#2C6FC4` |
| `barHigh` | `#007AFF` | `#0A84FF` |
| `barEmpty` | `#E5E5EA` | `#38383A` |

---

## 2. Типографика

Системный шрифт (SF Pro), без сторонних гарнитур. Размеры указаны в pt при базовом Dynamic
Type; масштабируются через `@ScaledMetric` относительно указанного `TextStyle`.
По ТЗ поддержка ограничена сверху размером XXL — ограничение ставится один раз на корневой
вьюхе: `.dynamicTypeSize(...DynamicTypeSize.xxLarge)`.

| Токен | Размер | Начертание | Трекинг | Relative to | Где применяется |
|---|---|---|---|---|---|
| `largeTitle` | 34 | bold | −0,6 | `.largeTitle` | заголовки вкладок |
| `wordDisplay` | 46 | semibold | −1,2 | `.largeTitle` | чешское слово на лицевой стороне |
| `wordDisplayCompact` | 38 | semibold | −1,0 | `.largeTitle` | слово в детальной карточке, вопрос в MC |
| `answerDisplay` | 32 | semibold | −0,7 | `.title` | перевод на обороте |
| `title1` | 30 | semibold | −0,8 | `.title` | чешское слово на обороте, вопрос в Typing |
| `title2` | 26 | bold | −0,5 | `.title2` | «Сессия завершена», cloze-предложение |
| `title3` | 22 | bold | −0,3 | `.title3` | подзаголовки секций, уровень в колоде |
| `statNumber` | 34 | semibold | −1,0 | `.title` | числа на карточке «Сегодня» (моноцифры) |
| `statNumberSmall` | 28 | semibold | −0,8 | `.title2` | плитки экрана прогресса (моноцифры) |
| `headline` | 17 | semibold | 0 | `.headline` | заголовок карточки, кнопки |
| `body` | 17 | regular | 0 | `.body` | строки списка, варианты ответа |
| `callout` | 16 | regular | 0 | `.callout` | строки настроек, категории |
| `subheadline` | 15 | regular | 0 | `.subheadline` | примеры предложений, пояснения |
| `footnote` | 13 | regular | 0 | `.footnote` | подписи под числами, счётчики |
| `caption` | 12 | regular | 0 | `.caption` | служебные пояснения под кнопками |
| `caption2` | 11 | semibold | 0 | `.caption2` | интервал над кнопкой оценки (моноцифры) |
| `sectionHeader` | 12 | semibold | +0,4 | `.caption` | заголовки групп, uppercase |
| `tabLabel` | 10 | medium | 0 | `.caption2` | подписи вкладок |

Моноширинные цифры (`.monospacedDigit()`) обязательны там, где число меняется на месте:
счётчик «осталось», таймер в режиме «Пары», интервалы над кнопками оценки, плитки прогресса.

---

## 3. Метрики

### 3.1. Отступы

| Токен | Значение | Где |
|---|---|---|
| `screenInset` | 16 | горизонтальные поля всех экранов |
| `cardPadding` | 20 | карточка «Сегодня», карточка слова в сессии |
| `cardPaddingCompact` | 16 | плитки, группы списка, график |
| `rowInset` | 16 | горизонтальный отступ строки списка |
| `separatorInset` | 16 / 58 | 58 — когда в строке есть иконка 30 pt |
| `sectionGap` | 26 | между смысловыми блоками экрана |
| `stackGap` | 12 | между карточками внутри блока |
| `tightGap` | 8 | между кнопками оценки, чипами |
| `safeTop` | 59 | верхний отступ контента (статус-бар не рисуем) |
| `safeBottom` | 40 | нижний отступ под кнопками сессии |

### 3.2. Радиусы

| Токен | Значение | Где |
|---|---|---|
| `chip` | 9 | чипы части речи, сегменты, бейджи-иконки |
| `field` | 11 | поле поиска |
| `button` | 14–15 | кнопки, варианты ответа, кнопки оценки |
| `card` | 16 | плитки, группы списка, график |
| `cardLarge` | 18 | карточка «Сегодня», карточка слова |
| `hero` | 22 | карточка в сессии |

Круглые элементы — половина высоты: кнопка озвучки 40, крупная кнопка прослушивания 116.

### 3.3. Высоты

| Токен | Значение | Комментарий |
|---|---|---|
| `listRow` | 46 | строка настроек и каталога |
| `buttonPrimary` | 50 | главная кнопка на карточке «Сегодня» |
| `buttonSession` | 54 | «Показать ответ», «Дальше», «Готово» |
| `gradeButton` | 62 | кнопка оценки (две строки) |
| `optionRow` | 62 | вариант ответа в MC |
| `matchTile` | 68 | плитка в режиме «Пары» |
| `tabBar` | 83 | 49 + зона домашнего индикатора |
| `progressBar` | 6 | прогресс-бар сессии, радиус 3 |
| `toggle` | 51 × 31 | системный `Toggle`, не рисуется вручную |
| `minTouchTarget` | 44 | нижняя граница для любой интерактивной области |

### 3.4. Размеры глифов

Шкала `AppFont` описывает текст; SF Symbol без подписи ею не покрывается, но и
числовым литералом во вьюхе быть не должен. Токены — в `AppSymbol`, применяются
модификатором `.appSymbol(_:)`, который масштабируется вместе с Dynamic Type.

| Токен | Значение | Где |
|---|---|---|
| `note` | 18 | иконка в callout примечания |
| `emptyState` | 28 | глиф пустого состояния в круге 64 pt |
| `placeholder` | 36 | глиф плейсхолдера картинки |
| `listen` | 44 | глиф в кнопке прослушивания 116 pt |
| `hero` | 48 | «Сессия завершена», «На сегодня всё» |

---

## 4. Компоненты

**Кнопка оценки.** Четыре в ряд, равной ширины, `gap: 8`. Сверху — интервал (`caption2`,
моноцифры, цвет «интервал»), снизу — подпись (`headline`, цвет «текст»). Фон — цвет оценки.
Интервалы рассчитывает `SRSScheduler` и передаёт готовой строкой; вьюха не считает сама.

**Строка списка.** Высота 46, инсет 16. Слева — необязательная иконка 30 × 30 с радиусом 8 и
фоном-тинтом, затем `callout`-заголовок, справа — значение (`callout`, `labelSecondary`) и
шеврон 15 pt. Разделитель начинается после иконки.

**Карточка в сессии.** Радиус 22, фон `surface`, паддинг 20–22, занимает всю свободную высоту
между шапкой и кнопками. Контент центрируется по вертикали на лицевой стороне и прижимается
к верху на обороте.

**Чип.** Радиус 9, паддинг 5 × 10, фон `fill` в светлой и `surfaceSecondary` в тёмной,
текст 12 semibold `labelSecondary`. Используется для части речи, рода/вида, уровня,
категории и родительного падежа.

**Callout примечания.** Радиус 14–15, фон `noteBackground`, рамка 1 pt `noteBorder`, иконка
18 pt слева, текст `subheadline` цветом `noteLabel`. Единственный способ показать `note`.

**Плейсхолдер картинки.** Радиус 14–16, фон `fillSecondary`, глиф 32–40 pt и подпись
`[картинка слова]` цветом `labelQuaternary`. Показывается всегда, когда `imageURL` пуст или
загрузка не удалась; карточка при этом не меняет размеров.

**Шапка сессии.** Круглая кнопка закрытия 32 pt, прогресс-бар, счётчик «осталось».
На лицевой стороне карточки под ней — разбивка очереди на повторения / новые / в изучении.

---

## 5. Доступность

Dynamic Type до XXL, ограничение ставится на корневой вьюхе. Все интерактивные элементы —
настоящие `Button`, `NavigationLink`, `TextField` с `Label`; минимальная область касания 44 pt.
Кнопки-иконки без текста обязаны иметь `accessibilityLabel`. Чешский текст помечается
языком `cs-CZ`, чтобы VoiceOver читал его чешским голосом, а перевод — языком интерфейса;
помощник для этого — `AppText.czech(_:)` в `Typography.swift`.

---

## 6. Соответствие макетов этапам ТЗ

| Этап ТЗ | Экраны холста |
|---|---|
| 1–2. Данные и ядро SRS | макеты не нужны |
| 3. Flashcards | «Учить — главная», «Карточка — лицо», «Карточка — ответ», «Итоги сессии» |
| 4. Каталог | «Словарь», «Карточка слова» |
| 5. Озвучка и картинки | кнопка озвучки и плейсхолдер на карточках |
| 6. Остальные режимы | «Выбор варианта», «Письменный ввод», «Аудирование», «Пары», «Карточка — cloze» |
| 7. Прогресс и настройки | «Прогресс», «Настройки», «На сегодня всё» |
| 8. Полировка | ряд «Тёмная тема» |
